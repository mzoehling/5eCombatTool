# Copy to .env.local and fill in.
# Base URL of the upstream JSON data source used by
# `npm run fetch-fixtures` (test fixtures) and `npm run build-srd` (SRD data).
# In CI this is provided as a repository secret.
UPSTREAM_DATA_URL=
